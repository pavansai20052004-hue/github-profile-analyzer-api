{
  "info": {
    "name": "GitHub Profile Analyzer API",
    "description": "Postman collection for testing the Node.js assignment API.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3000"
    },
    {
      "key": "username",
      "value": "octocat"
    }
  ],
  "item": [
    {
      "name": "Health Check",
      "request": {
        "method": "GET",
        "url": "{{baseUrl}}/health"
      }
    },
    {
      "name": "Analyze Profile",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"username\": \"{{username}}\"\n}"
        },
        "url": "{{baseUrl}}/api/profiles/analyze"
      }
    },
    {
      "name": "Analyze Profile By Path",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/api/profiles/{{username}}/analyze"
      }
    },
    {
      "name": "List Stored Profiles",
      "request": {
        "method": "GET",
        "url": "{{baseUrl}}/api/profiles?page=1&limit=20&sortBy=analyzedAt&order=desc"
      }
    },
    {
      "name": "Get Single Stored Profile",
      "request": {
        "method": "GET",
        "url": "{{baseUrl}}/api/profiles/{{username}}"
      }
    }
  ]
}
